---
title: Concepts MOC
type: moc
tags:
  - moc
---

# Concepts MOC

Distilled atomic ideas — auto-generated by `/distill-concepts` or the project engine.

## All concepts

```dataview
TABLE status, date, source_doc, length(distilled_from) AS "From N sources"
FROM "concepts"
WHERE type = "concept"
SORT file.name ASC
```

## Recently distilled

```dataview
TABLE distilled_from, source_doc, date
FROM "concepts"
WHERE type = "concept"
SORT date DESC
LIMIT 15
```

## Orphans (no outgoing links in Related — manual lint)

Browse graph view with filter `tag:#concept` for connectivity.

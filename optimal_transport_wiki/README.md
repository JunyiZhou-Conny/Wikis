# optimal_transport_wiki

Hand-curated research brain for **optimal transport** — foundations (Monge/Kantorovich),
entropic/Sinkhorn computation, Wasserstein & Gromov-Wasserstein distances, neural OT, and the
**balanced vs unbalanced** OT distinction.

- **Obsidian vault:** open `docs/` as the vault root.
- **Shared tooling:** uses the sibling `../.cursor/` rule (`wiki-standard.mdc`) and skills
  (`ingest-paper`, `distill-concepts`, `link-audit`, `daily-paper`).
- **Start at** `docs/maps/Home MOC.md` → `docs/maps/Optimal Transport MOC.md`.

See `AGENTS.md` for project rules and `docs/00-system/vault-contract.md` for note-writing
conventions and the credibility standard.
